# COE Implementation Baseline

Captured: 2026-07-28
Baseline audit link: pre-implementation COE gap audit at `/private/tmp/chip-service-coe-gap-audit.md`

## Canonical operating-system repository

- Path: `/Users/dan/lpos45`
- Isolated implementation worktree: `/Users/dan/lpos45-coe-v1`
- Remote: `https://github.com/listeningpost-dsteele/lposv3.git`
- Starting branch: `release/4.5.0`
- Starting commit: `9b8436d2d486b4a2231c9a698074d79f6cbc80fa`
- Implementation branch: `feature/coe-v1`
- Required historical commit `96e99b28b6b08d7b6d35c0415b3a386c35a8b3a4`: verified ancestor of the starting commit.
- Starting release worktree: clean when the isolated worktree was created.

The starting release branch already contained five local COE commits. Baseline review found that implementation was a local summary auditor, not the command-backed nine-gate system in the approved implementation order. It could report release ready with warning findings, stored JSONL instead of the v4 SQLite evidence model, lacked a complete application manifest, and did not power the deployed Chip route.

## Baseline commands

- Node: `v22.23.1`.
- npm: `10.9.8`.
- Python: `3.11.15`.
- `python3.11 verify_release.py`: exit 0, 358 immutable files, 45 specialists, 29 Standing Operations, 70 benchmarks, and 20 schemas.
- Canonical source suite with `PYTHONPATH=src`: exit 0 before implementation edits.

The first plain `python3.11 -m pytest` collection attempt used the wrong interpreter path and did not have the repository import path. It was discarded as environment evidence, its generated bytecode caches were removed, and the canonical command was rerun with the Hermes Python environment.

## Deployed-service baseline

The exact production-source review packet was preserved at `/private/tmp/chip-go-approval.IFAnza/REVIEW_PACKET.md`, SHA-256 `a57428875f999c05f9f37cbbb833fcc1da37a4e5b508627931a939a6ecde71af`. The corresponding read-only source snapshot is `/private/tmp/chip-go-approval.IFAnza/repo`.

Unauthenticated probes before implementation returned:

- `/`: HTTP 200, `Cache-Control: no-store`.
- `/dashboard/coe`: HTTP 200 with `Cache-Control: public, max-age=0`. This was an existing static page and did not satisfy private dashboard acceptance.
- `/ops/coe`: redirected to `/ops/sign-in`.
- `/api/v1/coe/summary`: HTTP 404.
- `/api/v1/operator/release-gate`: HTTP 401.

The production Google Cloud credential was expired during repository discovery. No deployment claim is based on that unavailable control-plane read. The exact recovered production snapshot and live HTTP probes are the deployment baseline until authenticated Google Cloud access is restored.

## Safety record

No force push, reset, clean, destructive stash, legacy-ledger migration, credential copy, or unrelated-data deletion was used. The original `/Users/dan/money-mission/chip-service` checkout remained untouched because it contained substantial preexisting work.
